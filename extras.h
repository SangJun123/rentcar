#ifndef _EXTRAS_H_
#define _EXTRAS_H_

#include "record.h"

// function prototypes
int defragment(Record[]);
void display_stats(Record[],int cnt);
void rent_a_car(Record records[],int cnt);
void sortlist(Record records[],int cnt);

#endif
